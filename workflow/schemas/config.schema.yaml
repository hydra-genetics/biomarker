$schema: "http://json-schema.org/draft-04/schema#"
description: snakemake configuration file
type: object
properties:
  resources:
    type: string
    description: Path to resources.yaml file
  samples:
    type: string
    description: path to samples.tsv file
  units:
    type: string
    description: path to units.tsv file
  default_container:
    type: string
    description: name or path to a default docker/singularity container
  reference:
    type: object
    properties:
      twist_dna_st_artifacts:
        type: string
        description: artifact file for twist dna st
      twist_dna_st_background_panel:
        type: string
        description: background noise file for twist dna st

  required:
    - default_container
    - resources
    - samples
    - units

  cnvkit2scarhrd:
    type: object
    description: parameters for cnvkit2scarhrd
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
        description: name or path to docker/singularity container

  finaletoolkit_end_motifs:
    type: object
    description: parameters for finaletoolkit_end_motifs
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
        description: name or path to docker/singularity container
      extra:
        type: string
        description: parameters that should be forwarded

  finaletoolkit_frag_length_bins:
    type: object
    description: parameters for finaletoolkit_frag_length_bins
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
        description: name or path to docker/singularity container
      extra:
        type: string
        description: parameters that should be forwarded

  finaletoolkit_interval_end_motifs:
    type: object
    description: parameters for finaletoolkit_interval_end_motifs
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
        description: name or path to docker/singularity container
      extra:
        type: string
        description: parameters that should be forwarded

  finaletoolkit_interval_mds:
    type: object
    description: parameters for finaletoolkit_interval_mds
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
        description: name or path to docker/singularity container

  finaletoolkit_mds:
    type: object
    description: parameters for finaletoolkit_mds
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
        description: name or path to docker/singularity container

  fix_scarhrd_output:
    type: object
    description: parameters for fix_scarhrd_output
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
        description: name or path to docker/singularity container

  fragle:
    type: object
    description: parameters for fragle
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
        description: name or path to docker/singularity container
      design_bed:
        type: string
        description: Path to the target regions BED file
      genome_build:
        type: string
        description: Reference genome build used for indexing alignment
      mode:
        type: string
        description: Run mode for FRAGLE inference
      outdir:
        type: string
        description: Output directory for FRAGLE results

  fragmentomics_fragment_length_patient_score:
    type: object
    description: parameters for fragmentomics_fragment_length_patient_score
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
        description: name or path to docker/singularity container
      extra:
        type: string
        description: parameters that should be forwarded

  fragmentomics_metrics:
    type: object
    description: Global parameters for fragmentomics metrics
    properties:
      canonical_cds_bed:
        type: string
        description: Path to canonical_cds_bed file
      exon_sizes:
        type: string
        description: Path to exon_sizes file
      tfbs_midpoints:
        type: string
        description: Path to tfbs_midpoints file
      atac_peaks:
        type: string
        description: Path to atac_peaks file
      exon_strand_mapping:
        type: string
        description: Path to exon_strand_mapping file
      min_mapq:
        type: integer
        description: Minimum mapping quality

  msisensor_pro:
    type: object
    description: parameters for msisensor_pro
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
        description: name or path to docker/singularity container
      extra:
        type: string
        description: parameters that should be forwarded

  msisensor_pro_filter_sites:
    type: object
    description: parameters for msisensor_pro_filter_sites
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
        description: name or path to docker/singularity container
      msi_sites:
        type: string
        description: Bed file for choosing specific msi sites to analyze. Set to empty string for no filtering.

  optitype:
    type: object
    description: parameters for optitype
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
        description: name or path to docker/singularity container
      extra:
        type: string
        description: parameters that should be forwarded
      sample_type:
        type: string
        description: -d for dna and -r for rna
      enumeration:
        type: integer
        description: number of output HLA-types, 1 equals output only the best

  scarhrd:
    type: object
    description: parameters for scarhrd
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
        description: name or path to docker/singularity container
      reference_name:
        type: string
        description: references name (grch37, grch38)
      seqz:
        type: boolean
        description: if sequeze file is produced by Sequenza or not (False, True)

  tmb:
    type: object
    description: parameters for tmb
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
        description: name or path to docker/singularity container
      af_lower_limit:
        type: number
        description: filter all variants with MAF < af_lower_limit
      af_upper_limit:
        type: number
        description: filter all variants with MAF > af_upper_limit
      af_germline_lower_limit:
        type: number
        description: filter all variants with af_germline_lower_limit > MAF < af_germline_upper_limit
      af_germline_upper_limit:
        type: number
        description: filter all variants with af_germline_lower_limit > MAF < af_germline_upper_limit
      artifacts:
        type: string
        description: file with artifact info created by the reference module. Set to empty string to skip, which is the default setting.
      background_panel:
        type: string
        description: file with background noise info created by the reference module. Set to empty string to skip, which is the default setting.
      background_sd_limit:
        type: integer
        description: filter all variants that are closer than background_sd_limit standard deviations from the background median noise. Set to arbitrary value if artifact file is not used.
      db1000g_limit:
        type: number
        description: filter all variants with found in 1000 Genomes witn MAF > db1000g_limit
      dp_limit:
        type: integer
        description: filter all variants with depth < dp_limit
      filter_genes:
        type: string
        description: path to file with gene names where variants should be filter out
      filter_nr_observations:
        type: integer
        description: filter all variants that has been observed before >= filter_nr_observations times in artifact file. Set to arbitrary value if artifact file is not used.
      filter_regions:
        type: array
        description: list of bedfiles
        items:
          type: string
          description: BED file with chromosomal regions that are used to filter out potential artifact variants
      gnomad_limit:
        type: number
        description: filter all variants with found in GnomAD witn MAF > gnomad_limit
      nr_avg_germline_snvs:
        type: number
        description: design dependent TMB correction term dependent on the average number of variants found in normal samples
      nssnv_tmb_correction:
        type: number
        description: design dependent TMB correction factor dependent on design size or correlation to commercial panels like TSO500
      variant_type_list:
        type: array
        description: list of variant types
        items:
          type: string
          description: Variant types that should be included in the in the TMB calculations ("missense_variant", "synonymous_variant", "stop_gained", "stop_lost")
      vd_limit:
        type: integer
        description: filter all variants with variants reads < vd_limit

  fragmentomics_metrics_get_bed_from_bam:
    type: object
    description: parameters for fragmentomics_metrics_get_bed_from_bam
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
      canonical_cds_bed:
        type: string
        description: Path to canonical_cds_bed file
      min_mapq:
        type: integer
        description: Minimum mapping quality

  fragmentomics_metrics_filter_comm_panel_genes:
    type: object
    description: parameters for fragmentomics_metrics_filter_comm_panel_genes
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container

  fragmentomics_metrics_gzip_comm_panel_genes:
    type: object
    description: parameters for fragmentomics_metrics_gzip_comm_panel_genes
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container

  fragmentomics_metrics_get_SE_fragstats:
    type: object
    description: parameters for fragmentomics_metrics_get_SE_fragstats
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container

  fragmentomics_metrics_calculate_SE:
    type: object
    description: parameters for fragmentomics_metrics_calculate_SE
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container

  fragmentomics_metrics_get_depth_fragstats:
    type: object
    description: parameters for fragmentomics_metrics_get_depth_fragstats
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container

  fragmentomics_metrics_calculate_normalized_depth:
    type: object
    description: parameters for fragmentomics_metrics_calculate_normalized_depth
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
      exon_sizes:
        type: string
        description: Path to exon_sizes file

  fragmentomics_metrics_calculate_frag_bins:
    type: object
    description: parameters for fragmentomics_metrics_calculate_frag_bins
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container

  fragmentomics_metrics_calculate_small_frags:
    type: object
    description: parameters for fragmentomics_metrics_calculate_small_frags
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container

  fragmentomics_metrics_calculate_full_gene_depth:
    type: object
    description: parameters for fragmentomics_metrics_calculate_full_gene_depth
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
      exon_sizes:
        type: string
        description: Path to exon_sizes file

  fragmentomics_metrics_get_left_4mer:
    type: object
    description: parameters for fragmentomics_metrics_get_left_4mer
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
      genome:
        type: string
        description: Path to genome file

  fragmentomics_metrics_get_right_4mer:
    type: object
    description: parameters for fragmentomics_metrics_get_right_4mer
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
      genome:
        type: string
        description: Path to genome file

  fragmentomics_metrics_calculate_MDS:
    type: object
    description: parameters for fragmentomics_metrics_calculate_MDS
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container

  fragmentomics_metrics_overlap_TFBS:
    type: object
    description: parameters for fragmentomics_metrics_overlap_TFBS
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
      tfbs_midpoints:
        type: string
        description: Path to tfbs_midpoints file

  fragmentomics_metrics_calculate_TFBS_entropy:
    type: object
    description: parameters for fragmentomics_metrics_calculate_TFBS_entropy
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container

  fragmentomics_metrics_overlap_ATAC:
    type: object
    description: parameters for fragmentomics_metrics_overlap_ATAC
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
      atac_peaks:
        type: string
        description: Path to atac_peaks file

  fragmentomics_metrics_calculate_ATAC_entropy:
    type: object
    description: parameters for fragmentomics_metrics_calculate_ATAC_entropy
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container

  fragmentomics_metrics_build_feature_tables:
    type: object
    description: parameters for fragmentomics_metrics_build_feature_tables
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container

  fragmentomics_metrics_extract_first_exon:
    type: object
    description: parameters for fragmentomics_metrics_extract_first_exon
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
      exon_strand_mapping:
        type: string
        description: Path to exon_strand_mapping file

  fragmentomics_metrics_build_combined_ft:
    type: object
    description: parameters for fragmentomics_metrics_build_combined_ft
    properties:
      benchmark_repeats:
        type: integer
        description: set number of times benchmark should be repeated
      container:
        type: string
        description: name or path to docker/singularity container
