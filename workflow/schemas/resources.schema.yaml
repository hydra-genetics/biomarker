$schema: "http://json-schema.org/draft-04/schema#"
description: snakemake configuration file
type: object
properties:
  default_resources:
    type: object
    properties:
      mem_mb:
        type: integer
        description: default memory in MB used for a rule
      mem_per_cpu:
        type: integer
        description: default memory in MB used per cpu for a rule
      partition:
        type: string
        description: default partition to use on the cluster for a rule
      threads:
        type: integer
        description: default number of threads that will be used by a rule
      time:
        type: string
        description: default max execution time for a rule
    required:
      - mem_mb
      - mem_per_cpu
      - partition
      - threads
      - time

  required:
    - default_resources

  cnvkit2scarhrd:
    type: object
    description: resource definitions for cnvkit2scarhrd
    properties:
      mem_mb:
        type: integer
        description: memory in MB used per cpu
      mem_per_cpu:
        type: integer
        description: memory used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  finaletoolkit_end_motifs:
    type: object
    description: resource definitions for finaletoolkit_end_motifs
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  finaletoolkit_frag_length_bins:
    type: object
    description: resource definitions for finaletoolkit_frag_length_bins
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  finaletoolkit_interval_end_motifs:
    type: object
    description: resource definitions for finaletoolkit_interval_end_motifs
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  finaletoolkit_interval_mds:
    type: object
    description: resource definitions for finaletoolkit_interval_mds
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  finaletoolkit_mds:
    type: object
    description: resource definitions for finaletoolkit_mds
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fix_scarhrd_output:
    type: object
    description: resource definitions for fix_scarhrd_output
    properties:
      mem_mb:
        type: integer
        description: memory in MB used per cpu
      mem_per_cpu:
        type: integer
        description: memory used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragle:
    type: object
    description: resource definitions for fragle
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available, minimum 2
      time:
        type: string
        description: max execution time

  fragmentomics_fragment_length_patient_score:
    type: object
    description: resource definitions for fragmentomics_fragment_length_patient_score
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_build_combined_ft:
    type: object
    description: resource definitions for fragmentomics_metrics_build_combined_ft
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_build_feature_tables:
    type: object
    description: resource definitions for fragmentomics_metrics_build_feature_tables
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_calculate_ATAC_entropy:
    type: object
    description: resource definitions for fragmentomics_metrics_calculate_ATAC_entropy
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_calculate_MDS:
    type: object
    description: resource definitions for fragmentomics_metrics_calculate_MDS
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_calculate_SE:
    type: object
    description: resource definitions for fragmentomics_metrics_calculate_SE
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_calculate_TFBS_entropy:
    type: object
    description: resource definitions for fragmentomics_metrics_calculate_TFBS_entropy
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_calculate_frag_bins:
    type: object
    description: resource definitions for fragmentomics_metrics_calculate_frag_bins
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_calculate_full_gene_depth:
    type: object
    description: resource definitions for fragmentomics_metrics_calculate_full_gene_depth
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_calculate_normalized_depth:
    type: object
    description: resource definitions for fragmentomics_metrics_calculate_normalized_depth
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_calculate_small_frags:
    type: object
    description: resource definitions for fragmentomics_metrics_calculate_small_frags
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_extract_first_exon:
    type: object
    description: resource definitions for fragmentomics_metrics_extract_first_exon
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_filter_comm_panel_genes:
    type: object
    description: resource definitions for fragmentomics_metrics_filter_comm_panel_genes
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_get_SE_fragstats:
    type: object
    description: resource definitions for fragmentomics_metrics_get_SE_fragstats
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_get_bed_from_bam:
    type: object
    description: resource definitions for fragmentomics_metrics_get_bed_from_bam
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_get_depth_fragstats:
    type: object
    description: resource definitions for fragmentomics_metrics_get_depth_fragstats
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_get_left_4mer:
    type: object
    description: resource definitions for fragmentomics_metrics_get_left_4mer
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_get_right_4mer:
    type: object
    description: resource definitions for fragmentomics_metrics_get_right_4mer
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_gzip_comm_panel_genes:
    type: object
    description: resource definitions for fragmentomics_metrics_gzip_comm_panel_genes
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_overlap_ATAC:
    type: object
    description: resource definitions for fragmentomics_metrics_overlap_ATAC
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  fragmentomics_metrics_overlap_TFBS:
    type: object
    description: resource definitions for fragmentomics_metrics_overlap_TFBS
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  msisensor_pro:
    type: object
    description: resource definitions for msisensor_pro
    properties:
      mem_mb:
        type: integer
        description: memory in MB used per cpu
      mem_per_cpu:
        type: integer
        description: memory used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  msisensor_pro_filter_sites:
    type: object
    description: resource definitions for msisensor_pro_filter_sites
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  optitype:
    type: object
    description: resource definitions for optitype
    properties:
      mem_mb:
        type: integer
        description: memory in MB used per cpu
      mem_per_cpu:
        type: integer
        description: memory used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  scarhrd:
    type: object
    description: resource definitions for scarhrd
    properties:
      mem_mb:
        type: integer
        description: memory in MB used per cpu
      mem_per_cpu:
        type: integer
        description: memory used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  tmb:
    type: object
    description: resource definitions for tmb
    properties:
      mem_mb:
        type: integer
        description: memory used
      mem_per_cpu:
        type: integer
        description: memory used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time
